name: Deploy Release

on:
  release:
    types: [released]
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      tag:
        type: string
        description: "Image tag to deploy (e.g. 1.0.0)"
        required: false
      latest:
        type: boolean
        description: "Tag image as latest"
        required: false
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-deploy
  cancel-in-progress: false

permissions: {}

jobs:
  deploy:
    name: Deploy
    uses: infinite-automations/workflows/.github/workflows/cd-docker-helm.yml@351e5260b800c2bf5e3c3437a5f8c7507bcc9399 # v1.0.0
    with:
      dry-run: ${{ github.event_name == 'pull_request' }}
      deploy-dry-run: ${{ vars.DRY_RUN_DEPLOY == 'true' }}
      source-tag: ${{ github.event.repository.default_branch || '' }}
      tags: ${{ github.event_name == 'pull_request' && 'dry-run' || github.event.inputs.tag || github.event.release.tag_name }}
      release-name: voting
      chart: stakater/application
      chart-version: "5.2.3"
      namespace: voting
      helm-repos: |
        stakater=https://stakater.github.io/stakater-charts
      values-files: |
        helm/common.yaml
      set-values: |
        ingress.hosts[0].host=${{ vars.FQDN }}
        ${{ github.event.inputs.tag && format('deployment.image.tag={0}', github.event.inputs.tag) || '' }}
      deploy-environment: ${{ (github.event_name == 'pull_request' || vars.DRY_RUN_DEPLOY == 'true') && 'dry-run' || 'k8s-deploy' }}
      deploy-runner: ${{ (github.event_name == 'pull_request' || vars.DRY_RUN_DEPLOY == 'true') && 'ubuntu-latest' || 'k8s-deploy' }}
    secrets:
      KUBECONFIG: ${{ secrets.KUBECONFIG }}
    permissions:
      contents: read
      packages: write
