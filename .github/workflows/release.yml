name: Release Application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-release
  cancel-in-progress: false

permissions: {}

jobs:
  ci:
    name: CI
    uses: infinite-automations/workflows/.github/workflows/ci-dotnet.yml@351e5260b800c2bf5e3c3437a5f8c7507bcc9399 # v1.0.0
    with:
      dry-run: ${{ github.event_name == 'pull_request' }}
      dotnet-version: "10.x"
      working-directory: src
      docker-context: .
      docker-target: ci
      semrel-extra-plugins: |
        semantic-release-replace-plugin
    secrets:
      APP_ID: ${{ vars.APP_ID_SEMREL }}
      APP_PRIVATE_KEY: ${{ secrets.APP_KEY_SEMREL }}
    permissions:
      actions: read
      checks: write
      contents: write
      issues: write
      packages: write
      pull-requests: write
      statuses: write
